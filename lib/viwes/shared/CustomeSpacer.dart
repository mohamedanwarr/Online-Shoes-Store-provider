import 'package:flutter/material.dart';
class CustomeSpacer extends StatelessWidget {
  const CustomeSpacer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
